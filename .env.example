# Локальная отправка заказов (vite dev). На хостинге используется public/api/config.php.
# Почта — обязательна, Telegram — дубль для менеджера.

EMAIL_TO=info@romedov.by
EMAIL_FROM=noreply@romedov.by

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
