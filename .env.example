# Заявки и заказы — только public/api/config.json (см. README). Файл .env для этого не нужен.

# Админка и Storage — через publishable key (см. scripts/supabase-storage.sql). Не нужен на Hoster.by.
# SUPABASE_SECRET_KEY=
